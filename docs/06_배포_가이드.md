# 배포 가이드

> 안드로이드 APK 빌드 및 스토어 배포

---

## 1. 안드로이드 APK (테스트용)

### 디버그 APK (빠른 테스트)
```bash
flutter build apk --debug
```
- 출력: `build/app/outputs/flutter-apk/app-debug.apk`
- 실기기에 USB 연결 후 `adb install` 또는 파일 전송 후 설치

### 릴리즈 APK
```bash
flutter build apk --release
```
- 서명 설정 필요 (`android/app/build.gradle.kts`)

---

## 2. 안드로이드 AAB (Play Store)

```bash
flutter build appbundle --release
```
- 출력: `build/app/outputs/bundle/release/app-release.aab`
- Play Console에서 업로드

---

## 3. iOS (App Store)

- Apple Developer 계정 ($99/년) 필요
- Xcode에서 서명/프로비저닝 설정
- `flutter build ios --release` 후 Xcode로 아카이브 → 업로드

---

## 4. 환경 변수 / 설정

- `google-services.json` (Android), `GoogleService-Info.plist` (iOS) 필수
- Firebase 프로젝트에 앱 등록 후 다운로드

---

## 5. 체크리스트 (배포 전)

- [ ] Firebase 프로덕션 프로젝트 설정
- [ ] 앱 아이콘/스플래시 적용
- [ ] 버전 코드/이름 설정 (`pubspec.yaml`, `build.gradle`)
- [ ] 서명 키스토어 (Android) 생성
- [ ] 개인정보처리방침 URL (스토어 필수)

---

## 6. 출시 게이트 6종 (보안/운영 필수)

- [ ] Rules + Functions 강제
  - [ ] `firebase deploy --only functions,firestore:rules`
  - [ ] 민감 액션(회비확정/예약성공/알림발송) 클라 직접 write 차단 확인
- [ ] App Check 강제 모드
  - [ ] Android/iOS/Web 등록 완료
  - [ ] Enforcement 전환 전 실패율 점검
- [ ] Idempotency + Rate Limit
  - [ ] 핵심 callable 중복요청 무해화
  - [ ] 짧은 시간 반복 호출 제한
- [ ] Audit + 실패율 대시보드
  - [ ] 감사로그 immutable 저장
  - [ ] 로그인/투표/알림/회비확정 실패율 추적
- [ ] 백업/복구 리허설
  - [ ] Firestore Export 스케줄
  - [ ] 복구 리허설 1회 기록
- [ ] CI 게이트 + Kill Switch
  - [ ] PR 머지 전 analyze/test/build 통과
  - [ ] 운영 중 위험 기능 즉시 비활성화 가능

---

## 7. 운영 문서 링크

- 보안 상세: [07_보안_가이드.md](07_보안_가이드.md)
- 장애 대응 절차: [08_운영_런북.md](08_운영_런북.md)
- 실패율 임계치: [09_실패율_임계치_가이드.md](09_실패율_임계치_가이드.md)
